import React from "react";

import { BsLink45Deg } from "react-icons/bs";

const ExperienceTemplate = ({ Icon, SkillName }) => {
  return (
    <div>
     
    </div>
  );
};

export default ExperienceTemplate;
